<UserControl x:Class="Lab4_PersonInfoList.Views.PersonListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Lab4_PersonInfoList.Views"
             mc:Ignorable="d" 
             DataContext="PersonListViewModel"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <TextBox Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center" FontFamily="Times New Roman" FontSize="20" Width="300" Margin="5" Text="{Binding FirstName, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}"></TextBox>
            <ComboBox FontFamily="Times New Roman" FontSize="20" Width="200" Margin="5" VerticalAlignment="Center">dfgdf</ComboBox>
            <ComboBox FontFamily="Times New Roman" FontSize="20" Width="80" Margin="5" VerticalAlignment="Center">2222</ComboBox>
        </StackPanel>
        <DataGrid Grid.Row="1">

        </DataGrid>
        <Button Grid.Row="2" Width="100" FontFamily="Times New Roman" FontSize="20" Margin="5">Add</Button>
    </Grid>
</UserControl>
